import React, {useState, useEffect} from 'react'

function Paths(){

  return(
    <div id='paths'>
      <p className='path-names'>home > termux > node.js</p>
    </div>
  )
}

export default(Paths)
